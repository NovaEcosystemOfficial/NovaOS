# NovaOS M0.1 — credentials (development image)

live/desktop user:  nova
password:           novaos

root password:      novaos

These credentials are set by configs/kiwi/novaos-m01/config.sh.
Change before any external distribution.
