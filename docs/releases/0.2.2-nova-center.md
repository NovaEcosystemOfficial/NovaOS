# Report Nova Center 0.2.2 (Sprint 16)

| Campo | Valore |
|-------|--------|
| Pacchetto | `nova-center` **0.2.2-1.nova** |
| Canale | `stable` (repo locale update-test) |
| Metodo | Overlay host + registrazione `localrpm` — **senza rebuild ISO** |
| Host | `novaos-fabio` |
| NovaOS | `NovaOS 0.2.1` |

## Deliverable

- Applicazione `desktop/nova-center/` (GUI GTK3, backend live, API `center.v1`)
- Launcher menu: `org.novaos.Center.desktop` → `nova-center`
- RPM: `packages/SPECS/nova-center.spec`
- Specifica: [`docs/platform/12-Nova-Center.md`](../platform/12-Nova-Center.md)
- Install host: `scripts/install-nova-center-host.sh`
- Ship script: `scripts/update-test/release-nova-center-0.2.2.sh`

## Verifiche

- [x] RPM costruito e pubblicato su `build/work/update-test/repo/channels/stable`
- [x] Installato su sistema live (`/usr/bin/nova-center`, Applications menu)
- [x] Snapshot `center.v1` con dati reali (CPU, RAM, disco, GPU, temp, batteria, rete, os-release)
- [x] Bridge Nova Update: servizio `attivo`, canale `stable`, backend `localrpm`
- [x] Servizi: `nova-updated` active; Ryuk / AI Core = `planned` (stub)
- [x] `nova-updater check` rileva `nova-center` dopo clear DB
- [x] `nova-updater apply` reinstalla il pacchetto

## Snapshot (estratto)

```
novaos_version: 0.2.1
hostname: novaos-fabio
kernel: 7.1.4-204.fc44.x86_64
cpu: Intel Core i7-4720HQ (8 cores)
memory: ~15.5 GiB
primary_ipv4: 192.168.1.51 (enp4s0f1)
update_service: attivo / channel stable
```

## Note

Ryuk non è implementato: solo voce catalogo e nota UI. Nessun mock nei collector.
