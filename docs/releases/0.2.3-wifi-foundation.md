# Report Sprint 17 — Nova Wi-Fi Foundation

| Campo | Valore |
|-------|--------|
| Host | `novaos-fabio` |
| Chipset | Qualcomm Atheros AR9462 (`ath9k`) → `wlp3s0` |
| Root cause | usrmerge rotto (`/usr/sbin` directory reale) → `wpa_supplicant` non avviabile |
| Fix | `/usr/sbin → bin` + NM policy + pacchetti ISO |

## Verifiche

- [x] Prima: `wlp3s0:wifi:unavailable` + errori D-Bus `wpa_supplicant`
- [x] Dopo: `wlp3s0:wifi:disconnected`, `wpa_supplicant` **active**
- [x] Scan AP funzionante (reti visibili)
- [x] Radio off/on → device non torna `unavailable`
- [x] Nova Center `wifi_details` (scheda, stato, MAC, radio, supplicant)
- [x] Autoconnect: nessun profilo Wi-Fi salvato sull’host di test; stack pronto — i profili creati da Plasma NM useranno autoconnect NM di default

## Comandi

```bash
bash scripts/fix-nova-wifi.sh
```

Docs: [`docs/platform/13-Nova-WiFi.md`](../platform/13-Nova-WiFi.md)
