# NovaOS M1 — Identity source assets

Official M1 branding generated from Design System **Luminous Precision**
(`docs/design-system/`).

| File | Role |
|------|------|
| `novaos-mark.svg` / `.png` | Abstract core + halo mark |
| `novaos-wordmark.svg` | Horizontal lockup |
| `novaos-wallpaper.png` | System wallpaper (ink field) |

Packaging copies live under `assets/`. KIWI overlay installs from
`configs/kiwi/novaos-m01/root/usr/share/`.

Colors: Ink `#070B12`, Stellar `#E8A54B`, Signal `#3DD6C6`.
