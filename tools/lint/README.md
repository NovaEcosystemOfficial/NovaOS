# lint /

| Tool | Role |
|------|------|
| `lint-workspace.sh` | Required dirs/files present |
| `validate-static.py` | XML/env/host-package/metalink static checks |

Used by `make lint` and `make validate`.
