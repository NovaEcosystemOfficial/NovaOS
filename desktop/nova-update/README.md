# Nova Update GUI

Applicazione GTK **Nova Update** (menu Applicazioni).

```bash
nova-update-gui
```

Mostra versione NovaOS, canale, stato `nova-updated`, lista pending,
cronologia, azioni Controlla / Installa.

Pacchetto RPM: `nova-update-gui` (SPECS/nova-update-gui.spec).
