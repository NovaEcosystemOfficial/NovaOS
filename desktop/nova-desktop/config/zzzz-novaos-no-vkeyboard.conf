# Always-on override (last alphabetically) — kill SDDM virtual keyboard.
# Survives older %config(noreplace) copies of zzz-novaos-desktop.conf.
[General]
InputMethod=
GreeterEnvironment=QT_IM_MODULE=compose,QT_VIRTUALKEYBOARD_DESKTOP_DISABLE=1,QT_QPA_PLATFORM=xcb
