# NovaOS session environment — branding + no greeter IM leakage into Plasma.
export NOVA_DESKTOP_VERSION=0.2.4
# Prefer NovaOS identity in About / info panels that honor os-release.
unset QT_IM_MODULE
